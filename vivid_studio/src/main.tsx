import React from "react";
import ReactDOM from "react-dom/client";
import VividStudio from "./VividStudio";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <VividStudio />
  </React.StrictMode>
);
