# shared_assets/ 폴더는 git으로 추적하되,
# 실제 미디어 파일(용량 큰 바이너리)은 제외한다.
*.mp4
*.mov
*.avi
*.mp3
*.wav
*.webm

# shared_fx/ 내 FX 코드(.tsx)는 글로벌 자산이므로 Git이 추적한다.
# 아래 규칙으로 .tsx 파일을 위 제외 목록에서 명시적으로 허용한다.
!shared_fx/
!shared_fx/**
!shared_fx/*.tsx
